/**
 * Legal/policy URLs. Hosted via GitHub Pages (`bemy-legal` repo) until
 * the user owns the `bemy.app` domain and can configure DNS. Reviewer
 * amendment §10: GitHub Pages is the locked-in default, NOT bemy.app.
 *
 * The user must enable GitHub Pages from the `bemy-legal/` directory at
 * the project root (see user-action checklist in the PostHog plan). Until
 * the URL resolves, App Store submission is blocked.
 */

export const PRIVACY_POLICY_URL =
  'https://jackosaurus.github.io/bemy-legal/privacy.html';

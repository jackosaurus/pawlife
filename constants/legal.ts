/**
 * Legal/policy URLs. Hosted via GitHub Pages (`pawlife-legal` repo) until
 * the user owns the `pawlife.app` domain and can configure DNS. Reviewer
 * amendment §10: GitHub Pages is the locked-in default, NOT pawlife.app.
 *
 * The user must enable GitHub Pages from the `pawlife-legal/` directory at
 * the project root (see user-action checklist in the PostHog plan). Until
 * the URL resolves, App Store submission is blocked.
 */

// Replace `<gh-user>` once the user enables GitHub Pages.
export const PRIVACY_POLICY_URL =
  'https://jackdinh.github.io/pawlife-legal/privacy.html';
